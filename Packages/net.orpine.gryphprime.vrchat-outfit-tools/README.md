# VRChat Outfit Tools

VCC/VPM package for Modular Avatar outfit-toggle menus.

For local development, copy this directory to a VRChat Unity project's `Packages/` folder and open the project in VCC to install its VPM requirements. Releases are intended as a VCC package ZIP; `.unitypackage` is the manual fallback.
